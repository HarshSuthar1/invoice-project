/*
  main.js
  Global bootstrap shared by every page that has a sidebar.

  Dark mode is now fully handled by the inline script in sidebar.php,
  so this file no longer needs to touch theme logic at all.
  It's kept for any future global JS that needs to run on every page.
*/

// Nothing to do here for dark mode — sidebar.php handles it inline.
// Add any other global page-level logic below if needed.